"""
Unit test package for AJAR

Run all tests in this package with `python -m unittest` (executed at the
project root directory).

All filenames must match the `test_*.py` pattern, otherwise they will not be
found and executed!
"""
